import { TokenPayloadInterface } from './token-payload.interface';

describe('TokenPayloadInterface', () => {
  it('should be defined', () => {
    expect(new TokenPayloadInterface()).toBeDefined();
  });
});
